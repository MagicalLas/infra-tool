[Unit]
Description=Prometheus
After=network.target

[Service]
ExecStart=/home/usada/bin/prometheus/prometheus --config.file /home/usada/infra/infra-tool/prometheus/config.yaml

User=usada
Group=usada
Restart=always

[Install]
WantedBy=multi-user.target
